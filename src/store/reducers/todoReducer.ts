// outer


//local
import {TodoActions, TodoState} from "../../types/todoReducerUtilites";


export const todoReducer = (state: TodoState, action: TodoActions): TodoState => {
    switch (action.type) {
        default:
            return state
    }
}
